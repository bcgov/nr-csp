{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Presets from https://github.com/bcgov/renovate-config",
  "extends": [
    "github>bcgov/renovate-config#2026.9.9"
  ],
  "packageRules": [
    {
      "description": "Freeze @bcgov-nr/nr-theme at 1.8.9 — 1.9.x changes global font rendering (BCSans). Do not auto-update.",
      "matchPackageNames": ["@bcgov-nr/nr-theme"],
      "enabled": false
    },
    {
      "description": "Hold typescript below v7 — the v7 native (Go) compiler ships no JS API, which typescript-eslint requires (peer range <6.1.0). Revisit once typescript-eslint supports TS 7.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7.0.0"
    },
    {
      "description": "Hold eslint below v10 — eslint-plugin-import (even latest 2.32.0) peers only up to eslint ^9. Revisit once eslint-plugin-import supports eslint 10.",
      "matchPackageNames": ["eslint", "@eslint/js"],
      "allowedVersions": "<10.0.0"
    },
    {
      "description": "Hold openpdf below v3 — v3 relocates com.lowagie.text to org.openpdf.text, but jasperreports-pdf (7.0.8, latest) still compiles against com.lowagie and brings openpdf 1.3.43.jaspersoft.2. A direct v3 pin wins dependency mediation, so Jasper PDF export fails at runtime. Revisit once JasperReports migrates to org.openpdf.",
      "matchPackageNames": ["com.github.librepdf:openpdf"],
      "allowedVersions": "<3.0.0"
    }
  ]
}
