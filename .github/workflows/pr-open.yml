name: PR

on:
  pull_request:

concurrency:
  group: ${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  # https://github.com/bcgov/action-builder-ghcr
  builds:
    name: Builds
    permissions:
      attestations: write
      id-token: write
      packages: write
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        package: [backend, frontend]
    timeout-minutes: 10
    steps:
      - uses: bcgov/action-builder-ghcr@cb2629351c87dd1c2130073e4ebb7233a9653a63 # v4.4.1
        with:
          package: ${{ matrix.package }}
          tags: ${{ github.event.number }}
          tag_fallback: latest
          triggers: ('${{ matrix.package }}/', '.github/workflows/')
          build_args: |
            BUILDKIT_INLINE_CACHE=1
            COGNITO_USER_POOL_ID=${{ vars.cognito_user_pool_id }}
            COGNITO_USER_POOL_CLIENT_ID=${{ vars.cognito_user_pool_client_id }}
            COGNITO_DOMAIN=${{ vars.cognito_domain }}
            COGNITO_OAUTH_SCOPES=${{ vars.cognito_oauth_scopes }}
            COGNITO_REDIRECT_SIGN_IN=https://${{ github.event.repository.name }}-${{ github.event.number }}.apps.gold.devops.gov.bc.ca/
            COGNITO_REDIRECT_SIGN_OUT=https://${{ github.event.repository.name }}-${{ github.event.number }}.apps.gold.devops.gov.bc.ca/logout
            VITE_MOCK_USER=${{ vars.vite_mock_user }}
            VITE_FAM_CLIENT_ID=${{ vars.vite_fam_client_id }}

  deploys:
    name: Deploys (${{ github.event.number }})
    needs: [builds]
    uses: ./.github/workflows/reusable-deploy.yml
    secrets:
      oc_namespace: ${{ secrets.oc_namespace }}
      oc_token: ${{ secrets.oc_token }}
      spring_datasource_url: ${{ secrets.spring_datasource_url }}
      spring_datasource_username: ${{ secrets.spring_datasource_username }}
      spring_datasource_password: ${{ secrets.spring_datasource_password }}
      jasper_server_username: ${{ secrets.jasper_server_username }}
      jasper_server_password: ${{ secrets.jasper_server_password }}
      jwt_jwks_uri: ${{ secrets.jwt_jwks_uri }}
      jwt_issuer: ${{ secrets.jwt_issuer }}
      jwt_audience: ${{ secrets.jwt_audience }}
    with:
      target: ${{ github.event.number }}
      triggers: ('backend/', 'frontend/', 'common/', '.github/workflows/')

  tests:
     name: Tests
     if: needs.deploys.outputs.triggered == 'true'
     needs: [deploys]
     uses: ./.github/workflows/reusable-tests.yml

  # ==========================================================================
  # WARNING: This job acts as the required merge gate for this workflow.
  # If you add a new job to this workflow, you MUST add its ID to the 'needs'
  # array below, otherwise its failure or cancellation will not block the PR!
  # ==========================================================================
  results:
    name: PR Results
    needs: [builds, deploys, tests]
    if: always()
    runs-on: ubuntu-slim
    timeout-minutes: 1
    steps:
      - name: Log Job Results Context
        run: |
          echo "=== Upstream Job Statuses ==="
          echo '${{ toJson(needs) }}'

      - name: Evaluate Overall Status
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
        run: |
          echo "❌ Critical Check Failure: At least one required job has failed or was cancelled."
          exit 1

      - name: Success Message
        run: echo "✅ All critical checks passed or were intentionally skipped!"
